## 1. SQLite 数据与查询

- [x] 1.1 在数据库初始化中幂等新增 `sync_file_events` 表、外键和映射/运行/排序索引，并验证旧数据库可无损升级
- [x] 1.2 实现批量写入新增与更新文件事件的方法，确保单次同步结果在一个事务中保存
- [x] 1.3 实现按映射查询每个相对路径最新文件记录，以及按同步运行查询文件记录的方法
- [x] 1.4 为文件名、同步时间的升降序白名单和稳定次级排序添加数据库单元测试

## 2. 同步任务关联与当前任务语义

- [x] 2.1 扩展同步任务状态以关联持久化 `run_id`，并在同步运行创建后安全更新该关联
- [x] 2.2 在同步成功处理 `SyncResult` 时记录 `files_added` 与 `files_updated`，排除跳过、删除和失败文件
- [x] 2.3 调整任务列表只返回 `queued` 和 `running` 状态，并保留同步成功/失败运行在历史列表中
- [x] 2.4 添加任务管理测试，覆盖运行 ID 关联、逐文件记录、失败不误记和完成任务移出当前任务

## 3. FastAPI 查询接口

- [x] 3.1 新增按同步映射查询已同步文件的接口，校验映射存在性、“同步映射”页面权限和安全排序参数
- [x] 3.2 新增按同步运行查询已同步文件的接口，校验运行存在性、“任务中心”页面权限和安全排序参数
- [x] 3.3 在任务和同步历史状态数据中返回 `run_id` 与文件记录可用标识，且不暴露不必要的绝对本地路径
- [x] 3.4 添加 API 单元/集成测试，覆盖有数据、空数据、旧历史、无权限、无效 ID 和非法排序参数

## 4. Vue 文件查看体验

- [x] 4.1 在同步映射卡片增加“查看”按钮，并实现可复用的同步文件弹窗状态与数据加载逻辑
- [x] 4.2 在任务中心当前同步任务和同步历史记录有文件时增加查看入口，并复用同一弹窗
- [x] 4.3 在弹窗展示文件名、相对路径、同步动作和北京时间，支持按文件名或同步时间切换升降序
- [x] 4.4 完善弹窗的加载、错误、无记录、关闭与同名文件展示状态，并适配现有页面布局
- [x] 4.5 执行前端构建并将最新构建产物同步到 FastAPI 提供的 `web_static` 目录

## 5. 验证、文档与交付

- [x] 5.1 更新 `docs/需求规格说明书.md`、`docs/设计说明书.md` 和 `README.md`，说明任务过滤、文件查看、排序、接口与运维行为
- [x] 5.2 检查并按本次数据库迁移、前端构建和测试需要更新 Jenkinsfile，同时保持首次部署与增量部署的数据目录规则
- [x] 5.3 运行后端单元测试、API/同步集成测试、前端构建和应用冒烟测试，修复全部失败
- [ ] 5.4 提交并推送 GitHub 后手动触发 Jenkins 构建，记录构建结果和部署后的服务访问验证信息
